# ==========================================
# Meta Campaign Manager - Docker Swarm Config
# ==========================================

# Domínio (sem https://)
DOMAIN=metamanager.seudominio.com

# ==========================================
# PostgreSQL (já existente na VPS)
# ==========================================
POSTGRES_HOST=postgres_postgres
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=7e7bd7ff077d8c083fdbb59dcfadec4e
POSTGRES_DB=metamanager

# ==========================================
# NextAuth
# ==========================================
# Gere com: openssl rand -base64 32
NEXTAUTH_SECRET=

# ==========================================
# Meta API (OPCIONAL - pode configurar via UI em /settings)
# ==========================================
# Se deixar vazio, configure após o deploy em:
# https://seudominio.com/settings → Meta API
META_ACCESS_TOKEN=
META_BUSINESS_ID=
META_AD_ACCOUNT_ID=
META_API_VERSION=v24.0

# ==========================================
# LLM Provider (obrigatório)
# ==========================================
# Suporta: OpenAI, OpenRouter, ou qualquer API compatível
#
# OpenAI (padrão):
#   LLM_API_KEY=sk-proj-xxx
#   LLM_BASE_URL=                     (deixe vazio)
#   LLM_MODEL=gpt-4o-mini
#
# OpenRouter:
#   LLM_API_KEY=sk-or-v1-xxx          (chave OpenRouter)
#   LLM_BASE_URL=https://openrouter.ai/api/v1
#   LLM_MODEL=openai/gpt-4o-mini      (ou anthropic/claude-3.5-sonnet, etc.)
LLM_API_KEY=sk-proj-xxx
LLM_BASE_URL=
LLM_MODEL=gpt-4o-mini

# ==========================================
# OpenAI - Transcrição de Áudio (Whisper)
# ==========================================
OPENAI_API_KEY=sk-xxx
WHISPER_MODEL=whisper-1

# ==========================================
# WhatsApp - Evolution API (OPCIONAL - configurar via UI)
# ==========================================
# Configure após o deploy em:
# https://seudominio.com/settings → WhatsApp
# EVOLUTION_API_URL=https://evolution.seudominio.com
# EVOLUTION_API_KEY=
# EVOLUTION_INSTANCE=

# ==========================================
# Registry (opcional - para build local)
# ==========================================
# REGISTRY=ghcr.io/guircosta
# TAG=latest
